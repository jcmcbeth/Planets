#ifndef _PLANETS_STRINGS_H_
#define _PLANETS_STRINGS_H_

/**
 * @brief Replace all instances of a character with another character.
 * @param buffer    String to replace the characters for.
 * @param replace   Character that should be replaced.
 * @param with      Character to use to replace the other character.
 */
void replace_char(char* buffer, char replace, char with);

#endif